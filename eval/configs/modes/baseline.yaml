# Baseline mode — no Arceus, pure mini-swe-agent (control group)
agent:
  agent_class: 'eval.agents.arc_agent.ArceusAgent'
  arc_mode: 'baseline'
  step_limit: 30
  cost_limit: 3.0

environment:
  environment_class: 'docker'
